import type { NextApiRequest, NextApiResponse } from "next";export default function handler(req: NextApiRequest, res: NextApiResponse) {  if (req.method === "POST") {    const body = typeof req.body === "string" ? (req.body.trim() ? JSON.parse(req.body) : {}) : (req.body ?? {});    return res.status(200).json({ ok: true, method: "POST", body, now: new Date().toISOString() });  }  return res.status(200).json({ ok: true, method: req.method, now: new Date().toISOString() });}